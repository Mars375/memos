# Memos Dev Log — 2026-04-07

## Session Cron memos — 02:58-03:25

### v0.12.0 Released
- 7 new CLI versioning commands: history, diff, rollback, snapshot-at, recall-at, version-stats, version-gc
- 9 new HTTP API endpoints for versioning (history, get version, diff, rollback, snapshot, recall-at, streaming recall-at, stats, GC)
- Flexible timestamp parsing (epoch, ISO 8601, relative like 1h/2d/1w)
- 53 new tests (34 CLI + 19 API), total 466 tests all passing
- Release: https://github.com/Mars375/memos/releases/tag/v0.12.0

### Key learnings
- CLI tests must mock _get_memos because each main() call creates a fresh MemOS instance
- API tests need direct storage upsert for version history (learn API creates new items, not versions)
- Stats keys are total_items (not tracked_items), no oldest_version or estimated_size_kb fields
