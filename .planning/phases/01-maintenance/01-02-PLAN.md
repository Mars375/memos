---
phase: 01-maintenance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ACTIVE.md
  - src/memos/miner/__init__.py
  - src/memos/miner/conversation.py
autonomous: true
requirements: [MAINT-05, MAINT-06]

must_haves:
  truths:
    - "ACTIVE.md references commits through April 13 2026 including dashboard modularization and all-in-one Docker"
    - "src/memos/miner/ directory no longer exists"
    - "All 1534 tests still pass after miner removal"
  artifacts:
    - path: "ACTIVE.md"
      provides: "Current project state reflecting April 13 commits"
      contains: "all-in-one Docker"
    - path: "src/memos/ingest/conversation.py"
      provides: "Active ConversationMiner (already exists, unchanged)"
      contains: "class ConversationMiner"
  key_links:
    - from: "src/memos/cli/commands_io.py"
      to: "src/memos/ingest/conversation.py"
      via: "import ConversationMiner"
      pattern: "from ..ingest.conversation import ConversationMiner"
---

<objective>
Update ACTIVE.md to current state and remove the orphaned miner/ directory.

Purpose: Eliminate stale documentation and dead code that confuse contributors and create false import targets.
Output: ACTIVE.md reflects April 13 state; src/memos/miner/ is deleted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ACTIVE.md to reflect April 13 state</name>
  <read_first>ACTIVE.md</read_first>
  <files>ACTIVE.md</files>
  <action>
Rewrite ACTIVE.md (per MAINT-05) to reflect the current project state as of April 13, 2026. The file currently stops at April 11 (P28 API Authentication). Update it with the following information derived from git log:

Key changes since last update:
- `c835166` feat: all-in-one Docker image + standalone compose profile
- `b5fb227` feat(dashboard): complete issue #39 — P2/P3 features
- `eb17ff6` fix: test and lint fixes for modular dashboard
- `4bf1e3c` refactor: split monolithic dashboard into modules (#40)
- `477bfc1` Merge feat/issue-36-canvas-graph: Canvas force-graph + P1/P2 features
- `9d3d90f` feat(dashboard): P2 time-lapse slider, health panel, link visibility fix
- `e1aaa8c` feat(dashboard): P1 — clustering, depth filter, hover tooltip, color modes
- `67c48c7` feat(dashboard): replace D3 SVG with force-graph Canvas — fix #36
- Plus fixes: #31 (Unicode speakers), #32 (text+content fields), #35 (host.docker.internal)
- Planning docs initialized (`.planning/` directory)

Update these sections:
1. **Statut**: Change session date to `2026-04-13`, mention dashboard modularization + Docker all-in-one as latest milestones
2. **Base**: Update to reflect current main branch state (v2.2.0, 1534 tests passing)
3. **Derniere action**: Summarize the dashboard refactor (#40), canvas force-graph (#36), all-in-one Docker, plus bug fixes (#31, #32, #35)
4. **OPEN / IN PROGRESS**: Update to reflect current state — P28 is merged, dashboard issues are done
5. **Prochaine etape**: Set to Phase 1 Maintenance (this plan set), then Phase 2 Dashboard P1

Keep the file in French (matching existing style). Keep the same markdown structure.
  </action>
  <verify>
    <automated>grep -q "2026-04-13" ACTIVE.md && grep -q "1534" ACTIVE.md && grep -qi "docker\|all-in-one" ACTIVE.md && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- ACTIVE.md contains date "2026-04-13"
- ACTIVE.md mentions "1534" tests
- ACTIVE.md references the all-in-one Docker image work
- ACTIVE.md references the dashboard modularization (#40)
- ACTIVE.md references the canvas force-graph (#36)
- File is written in French matching existing style
  </acceptance_criteria>
  <done>ACTIVE.md reflects project state through April 13 2026</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned src/memos/miner/ directory</name>
  <read_first>src/memos/miner/__init__.py, src/memos/miner/conversation.py, src/memos/ingest/conversation.py</read_first>
  <files>src/memos/miner/__init__.py, src/memos/miner/conversation.py</files>
  <action>
Per MAINT-06, remove the entire `src/memos/miner/` directory. This code is orphaned:

**Evidence it is safe to delete:**
- `src/memos/miner/conversation.py` (413 lines) is a duplicate of `src/memos/ingest/conversation.py` (which is the active version)
- No test files import from `memos.miner` (verified via grep)
- All CLI commands (`commands_io.py`) import from `..ingest.conversation` and `..ingest.miner`, NOT from `..miner`
- All API routes (`admin.py`) import from `...ingest.conversation`, NOT from `...miner`
- The only reference to `memos.miner.conversation` is inside `miner/conversation.py` itself (a self-referencing docstring example)
- `src/memos/miner/__init__.py` contains only a docstring, no re-exports used elsewhere

**Action:**
1. Delete the entire `src/memos/miner/` directory: `rm -rf src/memos/miner/`
2. Run the full test suite to confirm nothing breaks: `pytest -q --tb=short -x`
3. If any test fails due to the removal, investigate and fix the import (it should point to `ingest/` not `miner/`)

Do NOT modify any other files. The `ingest/` directory already has all the active code.
  </action>
  <verify>
    <automated>test ! -d src/memos/miner && pytest -q --tb=short -x 2>&1 | tail -3 | grep -q "passed" && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `src/memos/miner/` directory does not exist
- `src/memos/ingest/conversation.py` still exists and contains `class ConversationMiner`
- All 1534 tests pass (pytest exit code 0)
- No file outside `src/memos/miner/` was modified
  </acceptance_criteria>
  <done>Orphaned miner/ directory removed, all tests pass, ingest/ remains the single source of truth</done>
</task>

</tasks>

<verification>
```bash
# ACTIVE.md is current
grep "2026-04-13" ACTIVE.md
grep "1534" ACTIVE.md

# miner/ is gone
test ! -d src/memos/miner && echo "miner/ removed" || echo "FAIL: miner/ still exists"

# No dangling imports
grep -r "from memos.miner" src/ tests/ && echo "FAIL: dangling imports" || echo "No dangling imports"

# Active ingest still works
grep "class ConversationMiner" src/memos/ingest/conversation.py

# Full test suite
pytest -q --tb=short 2>&1 | tail -5
```
</verification>

<success_criteria>
- ACTIVE.md reflects April 13 state with all recent commits documented
- src/memos/miner/ is completely removed
- No imports reference memos.miner anywhere in src/ or tests/
- All 1534 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-maintenance/01-02-SUMMARY.md`
</output>
