---
phase: 01-maintenance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - src/memos/__init__.py
  - docker-compose.yml
  - .github/workflows/test.yml
autonomous: true
requirements: [MAINT-01, MAINT-02, MAINT-03, MAINT-04]

must_haves:
  truths:
    - "pip show memos-agent reports version 2.2.0"
    - "docker-compose.yml contains no :latest tags for third-party images"
    - "Every service in docker-compose.yml has logging.options with max-size and max-file"
    - "CI matrix includes Python 3.11, 3.12, and 3.13"
  artifacts:
    - path: "pyproject.toml"
      provides: "Project version set to 2.2.0"
      contains: 'version = "2.2.0"'
    - path: "src/memos/__init__.py"
      provides: "Package version 2.2.0"
      contains: '__version__ = "2.2.0"'
    - path: "docker-compose.yml"
      provides: "Pinned images and log limits on all services"
      contains: "chromadb/chroma:1.5.7"
    - path: ".github/workflows/test.yml"
      provides: "CI matrix with 3 Python versions"
      contains: "3.13"
  key_links:
    - from: "pyproject.toml"
      to: "src/memos/__init__.py"
      via: "version string must be identical"
      pattern: 'version = "2.2.0"'
---

<objective>
Fix version drift, pin Docker images, add log limits, and expand CI matrix.

Purpose: Eliminate release blockers and infrastructure debt so the codebase is safe for production and PyPI publishing.
Output: Four config files updated with concrete, verifiable values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync version and harden Docker Compose</name>
  <read_first>pyproject.toml, src/memos/__init__.py, docker-compose.yml</read_first>
  <files>pyproject.toml, docker-compose.yml</files>
  <action>
1. In `pyproject.toml`, change line `version = "1.0.0"` to `version = "2.2.0"` (per MAINT-01 — must match `__init__.__version__`). Do NOT touch `src/memos/__init__.py` — it already has `__version__ = "2.2.0"`.

2. In `docker-compose.yml`, replace all `:latest` tags for third-party images with pinned versions (per MAINT-02):
   - `chromadb/chroma:latest` -> `chromadb/chroma:1.5.7`
   - `qdrant/qdrant:latest` -> `qdrant/qdrant:v1.17.1`
   - Do NOT change `ghcr.io/mars375/memos:latest` — that is the project's own image and should stay as-is.

3. In `docker-compose.yml`, add JSON file log limits to EVERY service (per MAINT-03). Add this block to each service that lacks it:
   ```yaml
       logging:
         driver: json-file
         options:
           max-size: "10m"
           max-file: "3"
   ```
   Services to update: `memos-standalone`, `memos`, `chroma`, `memos-qdrant`, `qdrant`.
   Place the `logging:` block at the same indentation level as `image:`, `ports:`, etc. within each service.
  </action>
  <verify>
    <automated>grep -c 'version = "2.2.0"' pyproject.toml | grep -q 1 && grep -c ':latest' docker-compose.yml | grep -q 1 && grep -c 'max-size' docker-compose.yml | grep -q 5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `pyproject.toml` contains exactly `version = "2.2.0"`
- `docker-compose.yml` contains `chromadb/chroma:1.5.7` (not `:latest`)
- `docker-compose.yml` contains `qdrant/qdrant:v1.17.1` (not `:latest`)
- Only `ghcr.io/mars375/memos:latest` retains `:latest` (project's own image, 3 occurrences)
- All 5 services have `max-size: "10m"` and `max-file: "3"` in their logging config
  </acceptance_criteria>
  <done>Version aligned at 2.2.0, Docker images pinned, log limits on all services</done>
</task>

<task type="auto">
  <name>Task 2: Expand CI matrix to Python 3.13</name>
  <read_first>.github/workflows/test.yml</read_first>
  <files>.github/workflows/test.yml</files>
  <action>
In `.github/workflows/test.yml`, in the `test` job's `strategy.matrix.python-version` array (per MAINT-04):
- Change `["3.11", "3.12"]` to `["3.11", "3.12", "3.13"]`

That is the only change. Do not modify the lint job (it can stay on 3.11). Do not change any other workflow settings.
  </action>
  <verify>
    <automated>grep -A2 'python-version:' .github/workflows/test.yml | grep '3.13' && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
- `.github/workflows/test.yml` matrix contains exactly `["3.11", "3.12", "3.13"]`
- Lint job still uses `python-version: "3.11"` (unchanged)
- No other changes to the workflow file
  </acceptance_criteria>
  <done>CI matrix tests Python 3.11, 3.12, and 3.13</done>
</task>

</tasks>

<verification>
```bash
# Version sync
grep 'version = "2.2.0"' pyproject.toml
grep '__version__ = "2.2.0"' src/memos/__init__.py

# Docker pinning (only own image should have :latest)
grep -n ':latest' docker-compose.yml  # should show only ghcr.io/mars375/memos lines

# Log limits on all 5 services
grep -c 'max-size: "10m"' docker-compose.yml  # should be 5

# CI matrix
grep '3.13' .github/workflows/test.yml  # should match

# Tests still pass
pytest -q --tb=short -x 2>&1 | tail -5
```
</verification>

<success_criteria>
- `pyproject.toml` version = "2.2.0" matches `__init__.__version__`
- No third-party `:latest` in docker-compose.yml
- 5 services have log limits
- CI matrix includes 3.13
- All 1534 existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-maintenance/01-01-SUMMARY.md`
</output>
