---
phase: 02-dashboard-p1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memos/web/js/state.js
  - src/memos/web/js/filters.js
  - src/memos/web/js/graph.js
  - src/memos/web/dashboard.css
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03

must_haves:
  truths:
    - "Switching color mode to 'Cluster' visually recolors all nodes by their detected community"
    - "The legend dynamically shows cluster color dots when colorMode is 'cluster'"
    - "Nodes sharing the same namespace or primary tag are grouped into the same cluster even without direct edges"
  artifacts:
    - path: "src/memos/web/js/filters.js"
      provides: "Tag/namespace-seeded detectClusters() and rebuildLegend()"
      contains: "nodesByNS"
    - path: "src/memos/web/js/graph.js"
      provides: "colorMode-aware nodeCanvasObject fill logic"
      contains: "colorMode === 'cluster'"
    - path: "src/memos/web/dashboard.css"
      provides: "legend-dot style for cluster color chips"
      contains: ".legend-dot"
  key_links:
    - from: "src/memos/web/js/filters.js"
      to: "state.js clusterMap/clusterColors"
      via: "detectClusters() populates clusterMap and clusterColors"
      pattern: "clusterMap\\[id\\]=cid"
    - from: "src/memos/web/js/graph.js"
      to: "state.js colorMode/clusterMap/clusterColors"
      via: "nodeCanvasObject reads colorMode to pick fill color"
      pattern: "colorMode === 'cluster'"
    - from: "src/memos/web/js/filters.js"
      to: "dashboard.html #graph-legend"
      via: "rebuildLegend() dynamically populates legend div"
      pattern: "getElementById\\('graph-legend'\\)"
---

<objective>
Fix cluster coloring so it actually renders on the Canvas, upgrade detectClusters() to use tags and namespaces as grouping signal, and build a dynamic cluster legend.

Purpose: DASH-01/02/03 are the clustering feature set. The scaffolding exists but has three bugs/gaps: (1) nodeCanvasObject ignores colorMode, (2) detectClusters() uses only topology, (3) legend is hard-coded static HTML.

Output: Cluster color mode works visually, legend updates dynamically, and clusters reflect user's own tag/namespace organization.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-p1/02-RESEARCH.md

@src/memos/web/js/state.js
@src/memos/web/js/filters.js
@src/memos/web/js/graph.js
@src/memos/web/dashboard.css
@src/memos/web/dashboard.html

<interfaces>
<!-- Key globals from state.js that tasks will read/write -->
From src/memos/web/js/state.js:
  let clusterMap={};       // nodeId -> clusterId
  let clusterColors={};    // clusterId -> color
  let colorMode='namespace'; // 'namespace' | 'cluster' | 'tag' | 'layer'
  const LAYER_COLORS={0:'#22c55e',1:'#3b82f6',2:'#f97316',3:'#64748b'};

From src/memos/web/js/filters.js:
  const CLUSTER_PALETTE=[...12 colors...];
  function detectClusters() — BFS connected components, populates clusterMap/clusterColors
  function onColorModeChange(v) — sets colorMode, calls fg.nodeColor(fg.nodeColor())

From src/memos/web/js/graph.js:
  function nodeColorFn(n) — branches on colorMode correctly (lines 4-21)
  function nodeCanvasObject (lines 115-180) — BUG: fill color block (lines 136-142) ignores colorMode
  function initGraph() — calls buildGraphData(), creates ForceGraph instance

From src/memos/web/js/utils.js:
  function tc(str) — deterministic color from string
  function escHtml(str) — HTML entity escaping
  function nid(nodeOrId) — extract node ID from object or string
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix detectClusters() with tag/namespace seeding and add rebuildLegend()</name>
  <files>src/memos/web/js/filters.js, src/memos/web/dashboard.css</files>
  <read_first>src/memos/web/js/filters.js, src/memos/web/js/state.js, src/memos/web/dashboard.css, src/memos/web/dashboard.html</read_first>
  <action>
  In filters.js, modify `detectClusters()` (lines 8-39):

  1. At the top of the function (after line 8), add map reset to prevent stale entries:
     ```
     Object.keys(clusterMap).forEach(k => delete clusterMap[k]);
     Object.keys(clusterColors).forEach(k => delete clusterColors[k]);
     ```

  2. After the real-edge adjacency loop (after line 16, before the BFS loop), add virtual namespace edges:
     ```
     const nodesByNS = {};
     GD.nodes.forEach(n => {
       const ns = n.namespace || 'default';
       if (!nodesByNS[ns]) nodesByNS[ns] = [];
       nodesByNS[ns].push(n.id);
     });
     Object.values(nodesByNS).forEach(ids => {
       if (ids.length < 2) return;
       const anchor = ids[0];
       ids.slice(1).forEach(id => { adj[anchor].add(id); adj[id].add(anchor); });
     });
     ```

  3. After the namespace block, add virtual primary-tag edges:
     ```
     const nodesByTag = {};
     GD.nodes.forEach(n => {
       const pt = (n.tags || [])[0];
       if (!pt) return;
       if (!nodesByTag[pt]) nodesByTag[pt] = [];
       nodesByTag[pt].push(n.id);
     });
     Object.values(nodesByTag).forEach(ids => {
       if (ids.length < 2) return;
       const anchor = ids[0];
       ids.slice(1).forEach(id => { adj[anchor].add(id); adj[id].add(anchor); });
     });
     ```

  4. At the very end of `detectClusters()`, after the BFS loop closes (after line 39), call `rebuildLegend();`.

  5. Add a new function `rebuildLegend()` in filters.js (after `detectClusters()`). This function:
     - Gets `document.getElementById('graph-legend')`
     - Clears all children with `while (legend.firstChild) legend.removeChild(legend.firstChild);`
     - Rebuilds the 3 static edge-type legend items (mem, kg, highlight) using DOM createElement (NOT innerHTML with user data)
     - When `colorMode === 'cluster'`: adds cluster color dots. Extract unique cluster IDs from `Object.values(clusterMap)`, cap at 8 entries. Each entry: a `.legend-item` containing a `.legend-dot` div (background = clusterColors[cid]) and a span with text `Cluster N` (1-indexed). If more than 8 clusters, show a "+N more" item.
     - Use the exact pattern from the research section "Pattern: Dynamic Cluster Legend (DASH-02)"

  6. In `onColorModeChange()` (line 90-96), add `rebuildLegend();` after the fg reactive updates.

  7. In dashboard.css, add after the existing `.legend-line.highlight` rule (around line 97):
     ```
     .legend-dot{width:10px;height:10px;border-radius:50%;flex-shrink:0;}
     ```

  CRITICAL: Use DOM construction methods (createElement, textContent) for all legend items. Do NOT use innerHTML with any data that could contain user content (cluster labels derived from user tags).
  CRITICAL: Do NOT call initGraph() from detectClusters() or rebuildLegend(). These run during refreshGraph() before initGraph().
  </action>
  <verify>
    <automated>cd /home/orion/.openclaw/workspace-labs/forge/chantiers/memos && grep -c "nodesByNS" src/memos/web/js/filters.js && grep -c "nodesByTag" src/memos/web/js/filters.js && grep -c "rebuildLegend" src/memos/web/js/filters.js && grep -c "legend-dot" src/memos/web/dashboard.css</automated>
  </verify>
  <acceptance_criteria>
    - filters.js contains `Object.keys(clusterMap).forEach(k => delete clusterMap[k])` at the top of detectClusters()
    - filters.js contains `nodesByNS` variable building namespace groups
    - filters.js contains `nodesByTag` variable building primary-tag groups
    - filters.js contains a `rebuildLegend` function that calls `getElementById('graph-legend')`
    - rebuildLegend() is called at the end of detectClusters() AND inside onColorModeChange()
    - dashboard.css contains `.legend-dot{` rule with `border-radius:50%`
    - No innerHTML usage with user-supplied data in rebuildLegend()
  </acceptance_criteria>
  <done>detectClusters() groups nodes by namespace and primary tag before BFS, resets stale maps, and rebuilds the legend dynamically. CSS supports legend dot style.</done>
</task>

<task type="auto">
  <name>Task 2: Fix nodeCanvasObject to respect colorMode</name>
  <files>src/memos/web/js/graph.js</files>
  <read_first>src/memos/web/js/graph.js, src/memos/web/js/state.js</read_first>
  <action>
  In graph.js, replace the fill color block inside `nodeCanvasObject` (the block at lines 136-142 that currently reads):

  ```
  if(dimmed){
    ctx.fillStyle='rgba(30,33,56,0.5)';
  }else if(node.namespace&&node.namespace!=='default'){
    ctx.fillStyle=tc(node.namespace);
  }else{
    ctx.fillStyle='rgba(210,218,255,0.88)';
  }
  ```

  Replace with a colorMode-aware version that mirrors `nodeColorFn()` logic:

  ```
  let fillColor;
  if (dimmed) {
    fillColor = 'rgba(30,33,56,0.5)';
  } else if (colorMode === 'cluster') {
    fillColor = clusterColors[clusterMap[node.id]] || 'rgba(210,218,255,0.88)';
  } else if (colorMode === 'tag' && node.primary_tag) {
    fillColor = tc(node.primary_tag);
  } else if (colorMode === 'layer') {
    fillColor = LAYER_COLORS[node.layer !== undefined ? node.layer : 2] || '#64748b';
  } else {
    // namespace mode (default)
    fillColor = (node.namespace && node.namespace !== 'default')
      ? tc(node.namespace) : 'rgba(210,218,255,0.88)';
  }
  ctx.fillStyle = fillColor;
  ```

  This is a DIRECT replacement of lines 136-142. Keep all surrounding code (the ctx.beginPath/arc before, and the ctx.fill() after) unchanged.

  CRITICAL: Do NOT modify nodeColorFn() — it already works correctly. Only the Canvas draw code in nodeCanvasObject needs this fix.
  CRITICAL: Do NOT call initGraph() anywhere new. The existing onColorModeChange() in filters.js already calls fg.nodeCanvasObject(fg.nodeCanvasObject()) for reactive repaint.
  </action>
  <verify>
    <automated>cd /home/orion/.openclaw/workspace-labs/forge/chantiers/memos && grep -A 12 "Main circle" src/memos/web/js/graph.js | grep -c "colorMode"</automated>
  </verify>
  <acceptance_criteria>
    - The nodeCanvasObject fill block contains `colorMode === 'cluster'` branch
    - The nodeCanvasObject fill block contains `colorMode === 'tag'` branch
    - The nodeCanvasObject fill block contains `colorMode === 'layer'` branch
    - The namespace fallback is the else branch (default)
    - The `dimmed` check remains the first branch
    - nodeColorFn() is NOT modified
    - No new calls to initGraph() added
  </acceptance_criteria>
  <done>Switching the color mode selector to "Cluster" now visually recolors all nodes on the Canvas using clusterColors from detectClusters(). All four color modes (namespace, cluster, tag, layer) render correctly in both nodeColorFn and nodeCanvasObject.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "nodesByNS\|nodesByTag\|rebuildLegend" src/memos/web/js/filters.js` returns >= 3
2. `grep "colorMode === 'cluster'" src/memos/web/js/graph.js` returns a match inside nodeCanvasObject
3. `grep "legend-dot" src/memos/web/dashboard.css` returns a match
4. No `innerHTML` with user data in rebuildLegend (grep confirms DOM construction pattern)
</verification>

<success_criteria>
- Color mode "Cluster" produces visually distinct node colors on Canvas (DASH-01)
- Legend dynamically shows cluster color dots when in cluster mode, static edge items otherwise (DASH-02)
- Clusters group nodes sharing namespace or primary tag, not just topology (DASH-03)
- No initGraph() calls added (reactive repaint only)
- No new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-p1/02-01-SUMMARY.md`
</output>
