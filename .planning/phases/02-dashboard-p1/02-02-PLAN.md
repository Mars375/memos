---
phase: 02-dashboard-p1
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/memos/web/js/state.js
  - src/memos/web/js/graph.js
  - src/memos/web/js/panels.js
  - src/memos/web/js/filters.js
  - src/memos/web/dashboard.html
autonomous: true
requirements:
  - DASH-04
  - DASH-05
  - DASH-06
  - DASH-07

must_haves:
  truths:
    - "Depth slider visually dims nodes beyond the selected hop count from the focused node"
    - "A 'Local graph' button exists and, when clicked with a node selected, structurally filters to only that node and its 1-hop neighbors"
    - "Hovering a node shows tooltip with 150-char snippet, tags, namespace, importance, and separate in/out degree"
    - "Hovering a node highlights its edges even when another node was previously clicked"
  artifacts:
    - path: "src/memos/web/js/state.js"
      provides: "inDegreeMap, outDegreeMap, localGraphMode globals"
      contains: "inDegreeMap"
    - path: "src/memos/web/js/graph.js"
      provides: "rebuildDegreeMap with in/out, hover highlight fix, mouseleave handler"
      contains: "outDegreeMap"
    - path: "src/memos/web/js/panels.js"
      provides: "showTooltip with 150-char snippet and in/out degree"
      contains: "inDegreeMap"
    - path: "src/memos/web/js/filters.js"
      provides: "toggleLocalGraph() function"
      contains: "toggleLocalGraph"
    - path: "src/memos/web/dashboard.html"
      provides: "Local graph button in #controls"
      contains: "local-graph-btn"
  key_links:
    - from: "src/memos/web/js/panels.js"
      to: "state.js inDegreeMap/outDegreeMap"
      via: "showTooltip reads in/out degree maps"
      pattern: "inDegreeMap\\[node\\.id\\]"
    - from: "src/memos/web/js/filters.js"
      to: "graph.js buildGraphData/fg.graphData"
      via: "toggleLocalGraph filters structurally via fg.graphData()"
      pattern: "fg\\.graphData\\("
    - from: "src/memos/web/dashboard.html"
      to: "filters.js toggleLocalGraph"
      via: "onclick on local-graph-btn"
      pattern: "toggleLocalGraph\\(\\)"
---

<objective>
Complete depth navigation, add local graph button, upgrade hover tooltip with in/out degree, and fix hover edge highlighting.

Purpose: DASH-04/05/06/07 are the navigation and hover feature set. Depth filtering already works as visual dimming (keep as-is). Local graph needs a new button and structural filter. Tooltip needs richer content. Hover highlighting has a one-line bug.

Output: Full navigation controls (depth slider + local graph button) and rich hover experience (tooltip + edge highlight).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-p1/02-RESEARCH.md

@src/memos/web/js/state.js
@src/memos/web/js/graph.js
@src/memos/web/js/panels.js
@src/memos/web/js/filters.js
@src/memos/web/js/api.js
@src/memos/web/dashboard.html

<interfaces>
<!-- Key globals from state.js -->
From src/memos/web/js/state.js:
  let GD={nodes:[],edges:[],kgEdges:[]};
  let fg=null;
  let selId=null;
  let focusNode=null;
  let depthLimit=0;
  let highlightNodes=new Set(), highlightLinks=new Set();
  let hoverNode=null;
  let tooltipEl=null;
  const degreeMap={};

From src/memos/web/js/graph.js:
  function rebuildDegreeMap() — clears and repopulates degreeMap (lines 33-41)
  function buildGraphData() — returns {nodes, links} filtered by all criteria (lines 44-88)
  function initGraph() — creates ForceGraph instance (line 91)
  function clearAll() — resets selId, focusNode, highlights (lines 351-361)
  onNodeHover handler (lines 300-324) — BUG at line 307: `if(!highlightNodes.size||!selId)` suppresses hover when selId set

From src/memos/web/js/filters.js:
  function bfsNeighbors(startId, maxHops) — BFS from GD.nodes, uses fg.graphData().links (lines 42-67)
  function onDepthChange(v) — sets depthLimit, calls bfsNeighbors (lines 80-89)

From src/memos/web/js/panels.js:
  function showTooltip(node, x, y) — current: 120-char snippet, combined degree, no in/out split (lines 15-32)
  function hideTooltip() — hides tooltip element (lines 33-35)

From src/memos/web/js/utils.js:
  function nid(nodeOrId) — extract node ID
  function escHtml(str) — HTML entity escaping
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add in/out degree maps, local graph state, fix hover highlight, and add mouseleave handler</name>
  <files>src/memos/web/js/state.js, src/memos/web/js/graph.js</files>
  <read_first>src/memos/web/js/state.js, src/memos/web/js/graph.js, src/memos/web/js/api.js</read_first>
  <action>
  **A. state.js additions:**

  After the `const degreeMap={};` line (line 14), add:
  ```
  const inDegreeMap={};
  const outDegreeMap={};
  let localGraphMode=false;
  ```

  **B. graph.js — upgrade rebuildDegreeMap() (lines 33-41):**

  Replace the existing function with:
  ```
  function rebuildDegreeMap(){
    Object.keys(degreeMap).forEach(k=>delete degreeMap[k]);
    Object.keys(inDegreeMap).forEach(k=>delete inDegreeMap[k]);
    Object.keys(outDegreeMap).forEach(k=>delete outDegreeMap[k]);
    const all=[...GD.edges,...GD.kgEdges];
    all.forEach(e=>{
      const s=nid(e.source),t=nid(e.target);
      degreeMap[s]=(degreeMap[s]||0)+1;
      degreeMap[t]=(degreeMap[t]||0)+1;
      outDegreeMap[s]=(outDegreeMap[s]||0)+1;
      inDegreeMap[t]=(inDegreeMap[t]||0)+1;
    });
  }
  ```

  **C. graph.js — fix onNodeHover handler (lines 300-324):**

  The bug is at line 307: `if(!highlightNodes.size||!selId){`. This condition skips hover highlighting when `selId` is set (a node was previously clicked). Per DASH-07, hover must ALWAYS highlight edges of the hovered node.

  Replace the `onNodeHover` callback (lines 300-324) with:
  ```
  .onNodeHover(node=>{
    hoverNode=node;
    container.style.cursor=node?'pointer':null;
    if(node){
      showTooltip(node, window._lastMouseX||0, window._lastMouseY||0);
      // Always update hover edge highlight (DASH-07)
      highlightLinks=new Set();
      const conn=new Set([node.id]);
      const data2=fg.graphData();
      data2.links.forEach(l=>{
        const s=nid(l.source),t=nid(l.target);
        if(s===node.id){conn.add(t);highlightLinks.add(l);}
        if(t===node.id){conn.add(s);highlightLinks.add(l);}
      });
      highlightNodes=conn;
      fg.linkColor(fg.linkColor());
      fg.nodeColor(fg.nodeColor());
    }else{
      hideTooltip();
      if(!selId)clearAll();
    }
  })
  ```

  The key change: removed the `if(!highlightNodes.size||!selId)` guard. Hover now always populates highlightLinks and highlightNodes regardless of click state.

  **D. graph.js — add mouseleave handler in initGraph():**

  After the `container.addEventListener('mousemove', ...)` block (around line 101), add:
  ```
  container.addEventListener('mouseleave', () => {
    hideTooltip();
    if (!selId) clearAll();
  });
  ```

  This prevents tooltip from persisting when mouse quickly exits the graph container (Pitfall 4 from research).

  **E. graph.js — reset localGraphMode in clearAll():**

  In the `clearAll()` function (lines 351-361), add `localGraphMode=false;` after `focusNode=null;`. Also add local graph button opacity reset:
  ```
  const lgBtn=document.getElementById('local-graph-btn');
  if(lgBtn)lgBtn.classList.remove('active');
  ```

  CRITICAL: Do NOT modify the onNodeClick handler. It already works correctly.
  CRITICAL: Do NOT modify bfsNeighbors() — it is used correctly by onDepthChange().
  </action>
  <verify>
    <automated>cd /home/orion/.openclaw/workspace-labs/forge/chantiers/memos && grep -c "inDegreeMap" src/memos/web/js/state.js && grep -c "outDegreeMap" src/memos/web/js/graph.js && grep -c "mouseleave" src/memos/web/js/graph.js && grep "localGraphMode" src/memos/web/js/state.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - state.js contains `const inDegreeMap={};` and `const outDegreeMap={};` and `let localGraphMode=false;`
    - graph.js rebuildDegreeMap() clears and populates inDegreeMap and outDegreeMap
    - graph.js onNodeHover no longer has `if(!highlightNodes.size||!selId)` guard — hover always highlights
    - graph.js initGraph() has a `mouseleave` listener on container that calls hideTooltip()
    - graph.js clearAll() sets `localGraphMode=false` and removes 'active' class from local-graph-btn
  </acceptance_criteria>
  <done>In/out degree maps populated during rebuildDegreeMap(). Hover highlighting always fires (DASH-07 fix). Mouse leave cleans up tooltip. localGraphMode state ready for local graph feature.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade tooltip, add local graph button and handler</name>
  <files>src/memos/web/js/panels.js, src/memos/web/js/filters.js, src/memos/web/dashboard.html</files>
  <read_first>src/memos/web/js/panels.js, src/memos/web/js/filters.js, src/memos/web/js/graph.js, src/memos/web/js/state.js, src/memos/web/dashboard.html</read_first>
  <action>
  **A. panels.js — upgrade showTooltip() (lines 15-32):**

  Replace the entire `showTooltip` function with a DOM-construction version (no innerHTML with user data):

  ```
  function showTooltip(node,x,y){
    const tt=createTooltip();
    const inDeg=inDegreeMap[node.id]||0;
    const outDeg=outDegreeMap[node.id]||0;
    const imp=(node.importance||0).toFixed(2);
    const ns=node.namespace||'default';

    // Safe DOM construction
    while(tt.firstChild)tt.removeChild(tt.firstChild);

    const titleDiv=document.createElement('div');
    titleDiv.style.cssText='color:#a78bfa;font-weight:600;margin-bottom:4px;';
    titleDiv.textContent=(node.content||'').slice(0,40);
    tt.appendChild(titleDiv);

    const metaDiv=document.createElement('div');
    metaDiv.style.cssText='font-size:11px;color:#8b95b0;';
    metaDiv.textContent=ns+' \u00b7 in:'+inDeg+' out:'+outDeg+' \u00b7 imp:'+imp;
    tt.appendChild(metaDiv);

    const tags=(node.tags||[]);
    if(tags.length){
      const tagsDiv=document.createElement('div');
      tagsDiv.style.cssText='margin-top:4px;font-size:11px;color:#6ee7b7;';
      tagsDiv.textContent=tags.map(t=>'#'+t).join(' ');
      tt.appendChild(tagsDiv);
    }

    const snippet=(node.content||'').slice(0,150);
    if(snippet.length>40){
      const snippetDiv=document.createElement('div');
      snippetDiv.style.cssText='margin-top:6px;font-size:12px;color:#9ca3af;';
      snippetDiv.textContent=snippet+((node.content||'').length>150?'\u2026':'');
      tt.appendChild(snippetDiv);
    }

    tt.style.display='block';
    const px=Math.min(x+15,window.innerWidth-320);
    const py=Math.min(y+15,window.innerHeight-160);
    tt.style.left=px+'px';
    tt.style.top=py+'px';
  }
  ```

  Key changes from old version:
  - Snippet length: 120 -> 150 chars (DASH-06 requirement)
  - Degree: single combined -> separate `in:N out:N` display
  - Uses DOM createElement/textContent instead of innerHTML (security)
  - Reads `inDegreeMap` and `outDegreeMap` from state (added in Task 1)

  **B. filters.js — add toggleLocalGraph() function:**

  Add after the `onDepthChange()` function (after line 89):

  ```
  function toggleLocalGraph(){
    if(!focusNode)return;
    localGraphMode=!localGraphMode;
    const btn=document.getElementById('local-graph-btn');
    if(btn)btn.classList.toggle('active',localGraphMode);
    if(localGraphMode){
      // Compute 1-hop neighbors from FULL data (not filtered fg.graphData)
      const allData=buildGraphData();
      const adj={};
      allData.nodes.forEach(n=>{adj[n.id]=new Set();});
      allData.links.forEach(l=>{
        const s=nid(l.source),t=nid(l.target);
        if(adj[s])adj[s].add(t);
        if(adj[t])adj[t].add(s);
      });
      const neighbors=new Set([focusNode]);
      (adj[focusNode]||new Set()).forEach(nb=>neighbors.add(nb));
      const localNodes=allData.nodes.filter(n=>neighbors.has(n.id));
      const localLinks=allData.links.filter(l=>
        neighbors.has(nid(l.source))&&neighbors.has(nid(l.target))
      );
      fg.graphData({nodes:localNodes,links:localLinks});
      setTimeout(()=>{if(fg)fg.zoomToFit(400,40);},400);
    }else{
      initGraph();
    }
  }
  ```

  IMPORTANT: This computes BFS from `buildGraphData()` (which reads from GD), NOT from `fg.graphData()` which may already be filtered. This avoids Pitfall 5 from research.

  Also, in `refreshGraph()` in api.js — the executor should add `localGraphMode=false;` at the start of `refreshGraph()` to reset local mode on data refresh. Read api.js first to find the exact insertion point (should be near the top of the function, before any data fetching).

  **C. dashboard.html — add Local Graph button:**

  In dashboard.html, find the `<div id="controls">` section (line 188). After the existing buttons (zoomIn, zoomOut, resetZoom), add:
  ```
  <button class="cb" id="local-graph-btn" title="Local graph — neighbors only" onclick="toggleLocalGraph()">&#x25CE;</button>
  ```

  The `&#x25CE;` character is a bullseye symbol. The button uses the existing `.cb` class for consistent styling.

  CRITICAL: Do NOT modify onDepthChange() — it already works correctly as visual dimming (DASH-04 satisfied).
  CRITICAL: toggleLocalGraph() must use buildGraphData() for BFS, not fg.graphData() or bfsNeighbors().
  CRITICAL: Use DOM methods in showTooltip, no innerHTML with user content.
  </action>
  <verify>
    <automated>cd /home/orion/.openclaw/workspace-labs/forge/chantiers/memos && grep -c "inDegreeMap\|outDegreeMap" src/memos/web/js/panels.js && grep -c "toggleLocalGraph" src/memos/web/js/filters.js && grep -c "local-graph-btn" src/memos/web/dashboard.html && grep "slice(0,150)" src/memos/web/js/panels.js | head -1</automated>
  </verify>
  <acceptance_criteria>
    - panels.js showTooltip() uses `inDegreeMap[node.id]` and `outDegreeMap[node.id]` for separate in/out display
    - panels.js showTooltip() slices content to 150 chars (not 120)
    - panels.js showTooltip() uses DOM construction (createElement/textContent), NOT innerHTML with user data
    - panels.js showTooltip() displays namespace, in:N out:N, importance
    - filters.js contains `toggleLocalGraph` function that calls `buildGraphData()` (not `fg.graphData()` or `bfsNeighbors()`)
    - filters.js toggleLocalGraph sets `localGraphMode` and toggles 'active' class on button
    - dashboard.html contains `<button` with `id="local-graph-btn"` and `onclick="toggleLocalGraph()"`
    - api.js refreshGraph() sets `localGraphMode=false` at its start
  </acceptance_criteria>
  <done>Tooltip shows 150-char snippet, tags, namespace, importance, in/out degree (DASH-06). Local graph button exists and structurally filters to 1-hop neighborhood (DASH-05). Depth slider continues to work as visual dimming (DASH-04). Hover edge highlighting always works (DASH-07, fixed in Task 1).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -c "inDegreeMap\|outDegreeMap" src/memos/web/js/state.js` >= 2
2. `grep "slice(0,150)" src/memos/web/js/panels.js` returns a match
3. `grep "toggleLocalGraph" src/memos/web/js/filters.js` returns a match
4. `grep "local-graph-btn" src/memos/web/dashboard.html` returns a match
5. `grep -c "!highlightNodes.size||!selId" src/memos/web/js/graph.js` returns 0 (bug removed)
6. `grep "mouseleave" src/memos/web/js/graph.js` returns a match
</verification>

<success_criteria>
- Depth slider (1-5 hops) visually dims non-neighbor nodes around focused node (DASH-04 — existing behavior preserved)
- "Local graph" button structurally filters to node + direct neighbors when clicked (DASH-05)
- Hover tooltip shows: 150-char snippet, tags, namespace, importance, in-degree, out-degree (DASH-06)
- Hovered node edges always highlighted regardless of prior click state (DASH-07)
- No initGraph() calls for color-only changes
- No innerHTML with user data
- No new dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-p1/02-02-SUMMARY.md`
</output>
